// Simplified tensor examples using basic types for Phase 1
fn create_vector() -> int {
    return 5; // Vector length placeholder
}

fn create_matrix() -> int {
    return 6; // Matrix size placeholder (2*3)
}

fn main() -> int {
    let v_size = create_vector();
    let m_size = create_matrix();
    return v_size + m_size;
}

